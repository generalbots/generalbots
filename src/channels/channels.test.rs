#[cfg(test)]
mod tests {
    use super::*;
    use crate::tests::test_util;
    #[test]
    fn test_channels_module() {
        test_util::setup();
        assert!(true, "Basic channels module test");
    }
}
