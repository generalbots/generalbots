#[cfg(test)]
mod tests {
    use super::*;
    use crate::tests::test_util;
    #[test]
    fn test_config_module() {
        test_util::setup();
        assert!(true, "Basic config module test");
    }
}
