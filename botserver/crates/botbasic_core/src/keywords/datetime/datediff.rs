use botbasic_types::UserSession;
use botbasic_types::BasicRuntime;
use rhai::Engine;
use std::sync::Arc;

pub fn datediff_keyword(_state: &Arc<dyn BasicRuntime>, _user: UserSession, _engine: &mut Engine) {

}
